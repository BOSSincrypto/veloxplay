## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves. Skip if it is obvious from the title. -->

## Checklist

- [ ] `./gradlew assembleDebug lintDebug` passes locally
- [ ] Tested on a real device (state which Android version)
- [ ] No new dependency, or the new dependency is justified below
- [ ] Playback still starts, pauses and seeks without a visible delay

> Merging this into `main` publishes a signed APK to Releases automatically.
